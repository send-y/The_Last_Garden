# Технический план C1: фундамент строительства

- **Checkpoint:** C1 между 2B и 2C.
- **Рабочая ветка:** `codex/construction-foundation`.
- **Зависимости:** 2B и общая ортогональная сетка слиты в `main`.
- **Формат работы:** пользователь реализует маленькие реальные шаги по инструкции; агент проверяет архитектуру, тесты, документацию и GitHub.
- **Завершённые шаги:** C1a, визуальный клеточный курсор; C1b.1, сигнал выбора клетки; C1b.2, стабильный объект команды; C1b.3, детерминированный валидатор; C1c.1, коллекция чертежей и save v6; C1c.2, исполнение команды размещения; C1c.3, отображение сохранённых чертежей; C1d.1–C1d.3, команда, проверка и исполнение отмены; C1d.4, завершение чертежа в готовую стену.
- **Автоматическая проверка C1a:** импорт проекта, тесты симуляции и запуск Mechanics Lab пройдены.
- **Ручная проверка C1a:** подтверждена пользователем 28 июля 2026 года.
- **Ручная проверка C1b.1:** клики по миру передают координату, а элементы панели лаборатории не вызывают сигнал.
- **Автоматическая проверка C1b.2:** стабильные ID и целочисленная координата команды покрыты тестом.
- **Ручная проверка C1b.3:** свободная земля принимается, вода и существующие стены отклоняются.
- **Автоматическая проверка C1b.3:** свободная, заблокированная, выходящая за карту и поддельная команды покрыты тестами.
- **Завершённый шаг C1c.1:** пустая коллекция чертежей добавлена в состояние и save v6.
- **Автоматическая проверка C1c.1:** миграция v5→v6, повреждённое поле и изоляция getter покрыты тестами.
- **Завершённый шаг C1c.2:** симуляция исполняет только проверенную команду, добавляет канонический чертёж и отклоняет повторное размещение в занятой клетке.
- **Ручная проверка C1c.2:** первый чертёж добавляется в состояние, а повторный клик возвращает `core:occupied_cell`.
- **Автоматическая проверка C1c.2:** свободные, занятые, заблокированные и поддельные команды, несколько чертежей, нормализация и JSON round-trip покрыты тестами.
- **Завершённый шаг C1c.3:** отдельный `BlueprintLayer` отображает все чертежи из `Session` и перерисовывается после изменения или замены состояния.
- **Ручная проверка C1c.3:** сохранённый голубой чертёж остаётся на клетке после перемещения жёлтого курсора.
- **Автоматическая проверка C1c.3:** импорт, тесты, основная сцена и Mechanics Lab проходят в Godot 4.7.
- **Завершённый шаг C1d.1:** `ConstructionCommand.cancel_blueprint(cell)` формирует отдельную команду отмены по координате клетки.
- **Автоматическая проверка C1d.1:** участник, стабильный `action_id`, целочисленная клетка и отсутствие лишнего `building_id` покрыты тестом.
- **Завершённый шаг C1d.2:** валидатор отмены проверяет участника, действие, форму клетки и границы карты без доступа к состоянию.
- **Завершённый шаг C1d.3:** правая кнопка отправляет команду в симуляцию; существующий чертёж удаляется, а пустая клетка не изменяет состояние.
- **Ручная проверка C1d.3:** четыре чертежа последовательно удалены до нуля 29 июля 2026 года.
- **Автоматическая проверка C1d.2–C1d.3:** корректная, выходящая за карту, поддельная и пустая отмены, а также сохранность соседнего чертежа покрыты тестами.
- **Завершённый шаг C1d.4:** отдельная debug-команда переносит чертёж в коллекцию готовых конструкций save v7; `StructureLayer` рисует стену и создаёт физическую коллизию.
- **Ручная проверка C1d.4:** размещение, отмена и завершение стены подтверждены пользователем 29 июля 2026 года; счётчик меняется `blueprints 1→0`, `structures 0→1`.
- **Автоматическая проверка C1d.4:** миграция v6→v7, нормализация, JSON round-trip, занятость клетки, защита от завершения внутри персонажа и блокировка пути NPC покрыты тестами.
- **Следующее действие:** отдельное решение о готовности draft PR #7 к слиянию.
- **Статус checkpoint:** завершён в рабочей ветке.

## Цель

Проверить базовый язык строительства в духе колониальной симуляции: игрок выбирает клетку, размещает намерение-чертёж, а готовый объект возникает только после отдельного подтверждённого процесса.

Checkpoint не реализует комнаты, крыши, области, доставку материалов или работу NPC. Эти системы опираются на тот же контракт, но добавляются после проверки одной стены.

## Последовательность

### C1a. Визуальный клеточный курсор

`res://src/construction/construction_cursor.gd` подключён отдельным `Node2D` в Mechanics Lab:

- следует за мышью;
- привязывается к сетке 32×32;
- показывает ровно одну клетку;
- не изменяет симуляцию и сохранение;
- не перехватывает ввод панели лаборатории.

### C1b. Команда размещения чертежа

Левый клик создаёт не стену, а команду с координатой клетки и стабильным ID постройки. Симуляция проверяет границы, воду, существующие объекты и повторное размещение.

Промежуточный шаг C1b.1 завершён: `ConstructionCursor` испускает `cell_selected(cell)` через `_unhandled_input`, поэтому обработанные клики по debug-панели не становятся строительным вводом. Mechanics Lab временно печатает координату для проверки; это ещё не изменение состояния.

Шаг C1b.2 завершён: `ConstructionCommand.place_wall_blueprint(cell)` формирует словарь со стабильными `actor_id`, `action_id`, `building_id` и JSON-совместимым массивом координат. Словарь наблюдается в лаборатории, но пока не исполняется.

Шаг C1b.3 завершён: `ConstructionValidator` не доверяет входному словарю и проверяет участника, действие, тип постройки, форму координаты, границы карты и статические препятствия. Результат содержит стабильный `reason_id` и не изменяет состояние.

### C1c. Состояние и сохранение

Подтверждённый чертёж хранится в состоянии симуляции, переживает JSON round-trip и получает отдельное визуальное представление.

Шаг C1c.1 завершён: save v6 содержит коллекцию `blueprints`, старый save получает пустую коллекцию, а getter не раскрывает изменяемое состояние симуляции.

Шаг C1c.2 завершён: `execute_construction_command()` повторно использует детерминированный валидатор, проверяет занятость клетки и только затем добавляет запись со стабильными `building_id`, `cell` и `stage_id`. Загрузка принимает целые JSON-числа, удаляет повреждённые и повторяющиеся записи и восстанавливает каноническую форму чертежа.

Шаг C1c.3 завершён: `BlueprintLayer` является дочерним узлом настоящего `World`, читает снимок коллекции через `Session.get_blueprints()` и только рисует полупрозрачные клеточные обозначения. Сигналы `state_changed` и `state_reloaded` обновляют представление после исполнения команды и загрузки состояния.

### C1d. Отмена и завершение

Отмена удаляет чертёж через команду симуляции. Отдельное debug-действие завершает строительство; только готовая стена блокирует путь.

Шаг C1d.1 завершён: отмена представлена отдельной JSON-совместимой командой `core:cancel_blueprint`. Она указывает участника и клетку, но не изменяет коллекцию напрямую и не требует `building_id`, потому что в одной клетке может находиться только один чертёж.

Шаг C1d.2 завершён: `validate_cancel_blueprint()` проверяет входную команду, но намеренно не ищет чертёж — наличие записи остаётся ответственностью симуляции.

Шаг C1d.3 завершён: правая кнопка мыши создаёт команду отмены, а симуляция удаляет только совпавшую по клетке запись, сообщает `core:missing_blueprint` для пустой клетки и публикует `state_changed` только при реальном изменении.

Шаг C1d.4 завершён: нажатие колёсика в Mechanics Lab отправляет `core:complete_blueprint`. Симуляция удаляет исходный чертёж и создаёт отдельную запись `structures` со стадией `core:complete`. Save v7 сохраняет готовые конструкции; при загрузке готовая конструкция имеет приоритет над конфликтующим чертежом.

`StructureLayer` читает готовые конструкции через `Session`, рисует стеновые клетки и создаёт для них `StaticBody2D`. Только завершённая стена блокирует игрока и перестраивает клеточную навигацию NPC. Завершение отклоняется, если в клетке находится игрок или активный NPC.

## Архитектурные границы

- Курсор и спрайт чертежа являются представлением, а не источником истины.
- Координата клетки хранится как `Vector2i` в рантайме и как два целых числа в JSON.
- ID постройки использует namespace, например `core:wood_wall`.
- Проверка допустимости и изменение состояния принадлежат симуляции.
- Mechanics Lab использует настоящую игровую сцену и не создаёт параллельную строительную систему.
- Нажатие колёсика является только debug-заменой будущей доставки материалов и строительной работы.
- На C1 игрок выполняет действия сам; выбор строительной работы NPC относится к последующим шагам.

## Критерии C1a

1. Курсор точно совпадает с существующей сеткой карты.
2. При движении мыши внутри клетки курсор не дрожит.
3. При переходе границы курсор смещается ровно на 32 пикселя.
4. Запуск и существующие сценарии Mechanics Lab продолжают работать.
5. Курсор пока ничего не строит и не меняет сохранение.

## Критерии завершения C1

1. Чертёж и готовая конструкция хранятся раздельно и переживают сохранение.
2. Отмена удаляет только существующий чертёж.
3. Завершение переносит данные чертежа в готовую конструкцию.
4. Чертёж остаётся проходимым; готовая стена блокирует игрока и путь NPC.
5. Повторное размещение поверх чертежа или стены отклоняется.
6. Стена не завершается внутри персонажа.
